import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Codex S1 app shell. Content/auth wiring lands in S2+ (see PLANSET/05_SPRINTS_AND_WPS.md).
  reactStrictMode: true,
};

export default nextConfig;
